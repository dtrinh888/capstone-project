describe('Genre Detail Controller', function(){
	var ctrl, scope, $location, GenreDetailCtrl;

	beforeEach(module('MovieFinder'));

	it('should route to movie when selectMovie is called', inject(function($httpBackend, $rootScope, $route, $location){

	}));
});